<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <variable
            name="move"
            type="com.example.battlemanager.domain.model.MoveInfo" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/background_radius_5"
        app:typeBackground="@{move.type}"
        xmlns:app="http://schemas.android.com/apk/res-auto">
        <TextView
            android:id="@+id/item_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text='@{move.name == "미설정" ? "기술선택" : move.name}'
            android:textColor='@{move.name == "미설정" ? @color/grad_8 : @color/white}'
            android:textSize="18sp"
            android:gravity="center"
            android:layout_marginStart="10dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
